const Dashboard = () => {
  const styles = {
    dashboard__card: {
      display: "flex",
      height: "100vh",
    },
  };
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
};

export default Dashboard;
